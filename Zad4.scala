object Main extends App {
    def daSie(n: Int): Boolean = {
      def helper(x: Int, y: Int, i: Int, j: Int): Boolean = {
        //stuff here

      }
      def isEven(x: Int): Boolean = {
        if(x % 2 == 0) true
        else false
      }
      def isPrime(n: Int): Boolean = {      
        if(n == 0 || n == 1 || n == 2) return false
        while(y < (Math.sqrt(x)))
        {
          if(x % y == 0) return false
          else y += 1
        }
        if (y != x) return true
        }
    }
}